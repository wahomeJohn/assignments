class Student:
    def __init__(self,fname,lname,student_id,student_email,student_tel):
        self.fname          = fname
        self.lname          = lname
        self.student_id     = student_id
        self.student_email  = student_email
        self.student_tel    = student_tel
    def mailing_address(self,Adress,city,):

    def calculateScore(self):
        AverageScore = (quiz1 + quiz2 + quiz3 + test1 + test2)/5
        zoompoints = zoom1 + zoom2 + zoom3
        totalPoints = averageScore + zoompoints + assignmentPoints

        while student == A:
            if student == Male:
                if totalPoints >= 80:
                    return f"Awarded Scholarship"
                else:
                    return f"No scholarhip"
            else:
                if totalPoints >= 76:
                    return f"Awarded Scholarship"
                else:
                    return f"No Scholarship"